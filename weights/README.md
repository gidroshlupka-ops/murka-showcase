# Voice weights (not committed)

Drop your own RVC checkpoint here:

- `model.pth`
- `model.index` (optional but improves timbre lock)

Do not commit someone else's trained voice, or any weights you do not have
the right to redistribute. The API loads these paths from env (`MODEL_PATH`,
`INDEX_PATH`).
